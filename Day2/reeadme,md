version: xx.yy.zz

Major.minor.patch

Patch - Bug corrected, koi asar nhi padta update hone ke baad bhi 
minor - minor update, koi asar nhi padta update hone ke baad bhi 
major - remarkable change, prevuious code can become useless, malfunctioning

version: "^2.16.4" , ^ "Caret" tells that is can accept only minor and patch updates and ~ "till date" takes only patch updates, Agar major vale updates pe janaa hai to intensionally batana padega 

package-lock.json has got exact information of the version used in the project. If the project is run after two months, package-lock.json remembers which version was used in making.

If we have to share a project, node modules can not be shared to github, it is large in size, rather package.json and package-lock.json can be shared and after sharing in terminal, "npm install" it'll install the node modules package itself



CONCEPTUAL VIDEO, WATCH LAST 20 MINUTES AGAIN.



app.js comments

// import statement are in latest JS

// ReactDOM.render(element,document.getElementById('root'));  Doesn't work directly if using react 18

// render basically means on clicking a button, UI changes

// Dynamic data - changes freaquently, brought directly from the main server. like preference, data, likes etc. and the user for the first time when requires data from the main server, The CDN, close servers brings the data to the user and stores in themselves

// Static data - delivered by the closest servers, CDNs.

// Multiple elements can be rendered by constructing an array of the required elements

// Through JSX we can write html syntax in JS

// Bundler: Helps to optimize the code, and bundle the useful code. --- Webpack, parcel

// npm init

// npm install parcel

// npm : Node Package Manager(bs keh sakte hain)

// npm install react, npm install react-dom

// package.json has all the information abouit dependencies
